# Google Calendar API Configuration
# 
# Setup Instructions:
# 1. Go to https://console.cloud.google.com
# 2. Create a new project (or select an existing one)
# 3. Enable the Google Calendar API:
#    - Go to "APIs & Services" > "Library"
#    - Search for "Google Calendar API" and enable it
# 4. Create OAuth 2.0 credentials:
#    - Go to "APIs & Services" > "Credentials"
#    - Click "Create Credentials" > "OAuth client ID"
#    - Choose "Desktop app" as the application type
#    - Download the credentials JSON or copy the Client ID and Secret
# 5. Configure the OAuth consent screen:
#    - Go to "APIs & Services" > "OAuth consent screen"
#    - Add your email as a test user (for development)
# 6. Copy your Client ID and Client Secret below
# 7. Run `pnpm google-auth` to get your refresh token
# 8. Copy the refresh token to GOOGLE_REFRESH_TOKEN below

GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret-here
GOOGLE_REFRESH_TOKEN=your-refresh-token-here

# Calendar ID to fetch events from
# Use "primary" for the user's primary calendar
# Or use a specific calendar ID (found in Google Calendar settings)
GOOGLE_CALENDAR_ID=primary
